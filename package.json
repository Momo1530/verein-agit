{
  "name": "verein-agit",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "node scripts/tina-env-check.js dev -c \"next dev\"",
    "build": "node scripts/tina-env-check.js build --partial-reindex && next build",
    "start": "next start",
    "lint": "next lint",
    "tinacms-dev": "node scripts/tina-env-check.js dev -c \"next dev\"",
    "tinacms-build": "node scripts/tina-env-check.js build"
  },
  "dependencies": {
    "@tinacms/cli": "^3.1.0",
    "next": "14.2.3",
    "react": "^18",
    "react-dom": "^18",
    "tinacms": "^3.14.1"
  },
  "devDependencies": {
    "@types/node": "^20.14.0"
  }
}
